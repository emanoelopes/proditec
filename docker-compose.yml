version: '3.8'

services:
  app:
    build: .
    container_name: proditec-app
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m src.main --scraper basic # Default command, change as needed
  # Webapp (if migrated to src/webapp or similar)
  # webapp:
  #   build: .
  #   command: streamlit run src/webapp/home.py --server.address=0.0.0.0
